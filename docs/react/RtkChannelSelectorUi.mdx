---
title: RtkChannelSelectorUi
description: API reference for RtkChannelSelectorUi component (React Library)
---

# RtkChannelSelectorUi

## Properties

| Property | Type | Required | Default | Description |
|----------|------|----------|---------|-------------|
| `channels` | `ChatChannel[]` | ✅ | - | Channels |
| `iconPack` | `IconPack1` | ❌ | `defaultIconPack` | Icon pack |
| `selectedChannelId` | `string` | ✅ | - | Selected channel id |
| `showRecentMessage` | `boolean` | ✅ | - | show recent message in channel |
| `t` | `RtkI18n1` | ❌ | `useLanguage()` | Language |

## Usage Examples

### Basic Usage

```tsx
import { RtkChannelSelectorUi } from '@cloudflare/realtimekit-ui/react';

function MyComponent() {
  return <RtkChannelSelectorUi />;
}
```

### With Properties

```tsx
import { RtkChannelSelectorUi } from '@cloudflare/realtimekit-ui/react';

function MyComponent() {
  return (
    <RtkChannelSelectorUi
      channels={[]}
      selectedChannelId="example"
      showRecentMessage={true}
    />
  );
}
```

