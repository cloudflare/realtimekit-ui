---
title: rtk-channel-header
description: API reference for rtk-channel-header component (Angular Library)
---

# rtk-channel-header

## Properties

| Property | Type | Required | Default | Description |
|----------|------|----------|---------|-------------|
| `channel` | `ChatChannel` | ✅ | - | Channel object |
| `iconPack` | `IconPack1` | ❌ | `defaultIconPack` | Icon pack |
| `meeting` | `Meeting` | ✅ | - | Meeting object |
| `showBackButton` | `boolean` | ✅ | - | Show back button |
| `t` | `RtkI18n1` | ❌ | `useLanguage()` | Language |

## Usage Examples

### Setup

```typescript
// app.module.ts
import { CUSTOM_ELEMENTS_SCHEMA } from '@angular/core';
import { defineCustomElements } from '@cloudflare/realtimekit-ui/loader';

defineCustomElements();

@NgModule({
  schemas: [CUSTOM_ELEMENTS_SCHEMA]
})
export class AppModule { }
```

### Basic Usage

```html
<!-- component.html -->
<rtk-channel-header></rtk-channel-header>
```

### With Properties

```html
<!-- component.html -->
<rtk-channel-header
  [channel]=chatchannel
  [meeting]={meeting}
  [showBackButton]=true>
</rtk-channel-header>
```

